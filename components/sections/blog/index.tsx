'use client';

import Titlebar from "@/components/title-bar";

const Blog = () => {
    return (
        <div id="blogs" className="relative z-40 min-h-screen">
            <Titlebar title="Blogs" />
        </div>
    );
};

export default Blog;
